name: 'Changelog'
description: 'Create and commit changelog entry'
inputs:
  message:
    description: 'message test to use (*: prefix will be removed)'
    required: true
  pr_number:
    description: 'Pull request number'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
