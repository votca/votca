<options>

<mapchecker help="Outputs .pdb files for segments, qmmolecules and classical segments to check the mapping" section="sec:mapchecker">
    <qm_states help="qmstates states to check" default="n"></qm_states>
    <mp_states help="multipole states to check" default="e h"></mp_states>
    <map_file help="xml file with segment definition" default="votca_map.xml"></map_file>
    <md_pdbfile help="PDB file with the MD calculation" default="md_segments.pdb"></md_pdbfile>
    <qm_pdbfile help="PDB file with the QM calculation" default="qm_segments.pdb"></qm_pdbfile>
    <mp_pdbfile help="PDB file with the MP segments" default="mp_segments.pdb"></mp_pdbfile>
</mapchecker>

</options>
