<?xml version="1.0"?>
<options>
  <partialcharges help="Tool to derive partial charges from QM results stores in serialized file" section="sec:overlap">
    <esp_options help="options for the method">
      <esp2multipole>
        <state help="ground-,excited or transitionstate" default="n2s1" choices="groundstate,singlet,n2s1"/>
        <method help="Method to use derive partial charges" default="CHELPG" choices="mulliken,loewdin,CHELPG"/>
        <gridsize help="Grid accuracy for numerical integration within CHELPG" default="fine" choices="coarse,medium,fine"/>
      </esp2multipole>
    </esp_options>
  </partialcharges>
</options>
