<?xml version="1.0"?>
<options>
  <log2mps help="Generates an mps-file (with polar-site definitions) from a QM log-file" label="tool:log2mps">
    <job_name help="Input file name without extension, also used for intermediate files" default="system"/>
    <dftpackage help="QM package name" default="orca" choices="orca"/>
    <input help="dftpackage log file to read from, otherwise use job_name" default="OPTIONAL"/>
    <output help="MPs file with charges, if not given use job_name" default="OPTIONAL"/>
  </log2mps>
</options>
