<options>

<!-- xtp_run -e edft -f state.db -o options.xml -->
<xedft help="A wrapper for first principles based single site calculations" section="sec:xedft">

	<job_file help="Job job file (full path)"></job_file>

	<tasks help="What to run" default="input,run,parse">input,run,parse,project</tasks> 

	<dftpackage help="XML file with options for the first-principles package"></dftpackage>

	<store help="What to store" default="orbitals">orbitals</store>

</xedft>

</options>
