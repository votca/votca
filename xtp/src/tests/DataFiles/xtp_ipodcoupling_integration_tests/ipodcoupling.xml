<options>
<ipodcoupling help="Computes an electronic coupling between two, real, neighbor-list segments via POD2 (Projection Operator Diabatization), on a single, real, converged ground-state DFT calculation of the H-saturated supermolecule assembled from them -- mirrors IQM's own overall workflow (mapping, saturation-eligibility, H-saturation, relaxation, DFT), then hands off to PODCoupling for the actual coupling itself, instead of IQM's own dftcoupling/bsecoupling. See podcoupling.xml's own help text for the underlying POD2 method itself." section="sec:ipodcoupling">
	<job_file default="ipodcoupling.jobs" help="name of jobfile to which jobs are written">ipodcoupling.jobs</job_file>
	<map_file default="votca_map.xml" help="xml file with segment definition">system.xml</map_file>
	<tasks choices="[input,dft,parse,podcoupling]" default="input,dft,parse,podcoupling" help="tasks to perform during calculation">podcoupling</tasks>
	<dftpackage help="options for dftpackages">
		<name choices="xtp,orca" default="xtp" help="Name of the DFT package">xtp</name>
		<charge choices="int" default="0" help="Molecular charge">0</charge>
		<spin choices="int+" default="1" help="Molecular multiplicity">1</spin>
		<basisset default="def2-tzvp" help="Basis set for MOs">3-21G</basisset>
		<auxbasisset default="OPTIONAL" help="Auxiliary basis set for RI">aux-def2-svp</auxbasisset>
		<functional default="XC_HYB_GGA_XC_PBEH" help="Exchange correlation functional used. You can also specify an exchange and a correlation functional">XC_HYB_GGA_XC_PBEH</functional>
	</dftpackage>
	<store_dft choices="bool" default="false" help="If true, writes the converged orb file (the full, H-saturated supermolecule) to disk after the DFT step">false</store_dft>
	<include_linkers choices="bool" default="false" help="If true, looks for real, actual linker segments genuinely, covalently connecting the two, real, neighbor-list segments (via Topology::FindLinkingSegments, real bond connectivity only -- no segment-type whitelist at all, unlike IQM's own linker_names), PBC-correctly positions them, and includes them in the supermolecule -- their own atoms belong to NEITHER fragment_A_atoms nor fragment_B_atoms in the resulting PODCoupling calculation. Default off, matching IQM's own default-off linker behavior.">false</include_linkers>
	<podcoupling help="Options for the actual POD2 coupling calculation itself, once the H-saturated supermolecule's own DFT is converged">
		<levA choices="int+" default="1" help="Number of orbitals to cover for fragment A, counting outward from its own HOMO/LUMO boundary, covering BOTH occupied (hole-transport) and virtual (electron-transport) orbitals together in one calculation -- same convention as DFTcoupling's own levA/podcoupling.xml's own levA exactly. 1 covers just {HOMO, LUMO}; 2 covers {HOMO-1, HOMO, LUMO, LUMO+1}; etc.">1</levA>
		<levB choices="int+" default="1" help="Same as levA, for fragment B">1</levB>
	</podcoupling>
</ipodcoupling>
</options>

