<?xml version="1.0"?>
<options>
  <partialcharges help="Tool to derive partial charges from QM results stores in serialized file" section="sec:overlap">
      <esp2multipole link="esp2multipole.xml"/>
      <job_name help="Input file name without extension, also used for intermediate files" default="system"/>
      <input help="orbfile to read from, otherwise use job_name" default="OPTIONAL"/>
      <output help="mps to write to, otherwise use job_name" default="OPTIONAL"/>
  </partialcharges>
</options>
